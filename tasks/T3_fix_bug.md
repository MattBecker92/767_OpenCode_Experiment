# Task 3 — Fix a Bug

The test `project/tests/test_scheduler.py::test_run_empty` is currently failing.

Find the bug in `project/schedlib/scheduler.py` and fix it so that running a scheduler with no jobs works correctly.

Run the tests to confirm the fix works before finishing.