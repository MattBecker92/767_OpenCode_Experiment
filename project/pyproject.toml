[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "schedlib"
version = "0.1.0"
description = "A simple job scheduler library"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
